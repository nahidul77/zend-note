<?php

/**
 * fopen(filepath, mode), return a file handle or file reference which file we will working on.
 * 
 * r (Read from start, must exist)
 * w (Truncate/write from start)
 * a (Append/ write from end)
 * 
 * recommendation
 * 
 * w: strating a new file
 * r+: reading/writing to an existing file
 * r: read only access to a file
 * a: appending data to the end of a file
 */
