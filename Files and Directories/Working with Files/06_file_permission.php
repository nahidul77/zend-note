<?php

/**
 * File Permissions
 * 
 * Files and directories belogn to a user
 * web server is often a different user
 * Permission determine who can access files
 * cannot read or write a file without file permissions
 * cannot create or delete a file without directory permissions
 * Setting permissions varies by operating system
 * Windows GUI: Properties > Security > Edit
 * Unix: chmod 
 * rwx(user)rwx(group)rwx(other)
 */
